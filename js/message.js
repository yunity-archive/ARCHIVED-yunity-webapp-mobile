//import toastr from 'toastr';

export var yMsg = {
  success: (msg, title) => {
    //toastr.success(msg, title);
    alert(msg);
  },
  error: (msg, title) => {
    //toastr.error(msg, title);
    alert(msg);
  }
};